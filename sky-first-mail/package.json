{
  "name": "sky-first-mail",
  "version": "7.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "validate": "node --check src/index.js && node --check src/lib/auth.js && node --check src/lib/http.js && node --check src/lib/mail.js && node --check src/lib/security.js && node --check public/app.js && node --check public/v3.js && node --check public/v4.js && node --check public/v5.js"
  },
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
